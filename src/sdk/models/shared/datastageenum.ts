import { SpeakeasyBase, SpeakeasyMetadata } from "../../../internal/utils";

export enum DataStageEnum {
    Daily = "daily",
    Historical = "historical",
    Manual = "manual",
    Webhook = "webhook"
}